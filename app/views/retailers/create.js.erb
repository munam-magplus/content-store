var row_count = $('#searchResultGrid').size();
if(row_count == 0)
{
  $('#result').html('<%= j(render partial: 'post_create', locals: { retailer: @retailer  }) %>')
}
  else
{
  $('#result').append('<%= j(render partial: 'post_create_append', locals: { retailer: @retailer  }) %>')
}
