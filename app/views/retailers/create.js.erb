var row_count = $('#searchResultGrid').size();
if(row_count == 0)
{
  $('#result').html('<%= j(render partial: 'result', locals: { retailer: @retailer  }) %>')
}
  else
{
  $('#result').append('<%= j(render partial: 'result_append', locals: { retailer: @retailer  }) %>')
}
