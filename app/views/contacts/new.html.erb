<html>
  <body class = "body1">
    <div class="row">
      <div class="col-md-12"><h4 class="borderBottom">Contact Us</h4>
      * Indicates required field
      <% flash.each do |key, value| %>
          <div class="alert alert-<%= key %>"><%= value %></div>
        <% end %>
      <%= render 'form', contact: @contact %>
      </div>
    </div>
  </body>
</html>

